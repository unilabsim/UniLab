# @package _global_
defaults:
  - override /reward: g1_motion_tracking

training:
  task_name: G1MotionTracking
algo:
  num_envs: 1024
  max_iterations: 10000
  save_interval: 100
  obs_groups:
    default:
      - policy
    actor:
      - actor
  algorithm:
    entropy_coef: 0.005
