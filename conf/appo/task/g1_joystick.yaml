# @package _global_
defaults:
  - override /reward: g1_appo_mujoco

training:
  task_name: G1JoystickFlatTerrain
algo:
  max_iterations: 500
  save_interval: 100
motrix_legacy:
  enabled: true
  env_cfg_override:
    domain_rand:
      randomize_kp: false
      randomize_kd: false
